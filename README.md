DISCLAIMERS :
i) Dataset is made temporarily in small scale by using random images from the internet .
ii) All the embeddings, datasets etc are linked to the code via google drive . 
iii) Function of the files are as follows :
Dataset_creation -- creating the dataset as a csv file from raw images 
Embeddings -- generates embeddings from the images in the dataset 
Matching_recorded -- performs search via the recorded feed 
Matching_cctv -- performs matches via live cctv feed 
Matching -- recovery file (not to be used)
